---
const { src, ...attrs } = Astro.props;

import styles from "./styles.module.scss";
---

<figure class={styles.figure}>
  <video src={src} class={styles.video} autoplay loop muted playsinline></video>
  <figcaption class={styles.caption}>{attrs.alt}</figcaption>
</figure>
