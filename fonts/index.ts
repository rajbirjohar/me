import { Inter, Besley } from "next/font/google";

const sanserif = Inter({ subsets: ["latin"], variable: "--sanserif" });
const serif = Besley({ subsets: ["latin"], variable: "--serif" });

export { sanserif, serif };
